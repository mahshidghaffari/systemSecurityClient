// import { services } from "../Services/Services";

const state = {
  test: "test",
};

const mutations = {};

// dispatch action
const actions = {};

export const firstModule = {
  namespaced: true,
  state,
  mutations,
  actions,
};
